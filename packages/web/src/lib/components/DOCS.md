# Components

Shared Svelte UI building blocks.

## Use

- Prefer local primitives in `ui/`
- Prefer `phosphor-svelte` for icons
- Trust component styling, only override classes when necessary
- `session/` owns chat/session interaction surfaces.

